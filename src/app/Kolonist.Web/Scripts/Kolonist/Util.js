var Kolonist;
(function (Kolonist) {
    var Util = {

        Degree2Rad: function (degree) {
            return Math.PI / 180 * degree
        }

    };
    Kolonist.Util = Util;
})(Kolonist || (Kolonist = {}));